POSTGRES_USER=music
POSTGRES_PASSWORD=replace-with-strong-password
POSTGRES_DB=music
JWT_SECRET=replace-with-random-256-bit-secret

# Local development defaults
PORT=3000
UPLOADS_DIR=/app/uploads
VITE_API_BASE_URL=/api
VITE_PROXY_TARGET=http://backend:3000

# Optional SMTP email verification (recommended for production)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=Neferkey Music App
EMAIL_VERIFICATION_URL_BASE=
